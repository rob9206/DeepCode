# Python Hello World App

## Description
This is a simple Python application that prints 'Hello, World!' to the console.

## How to Run
To run the application, navigate to the directory containing `main.py` and execute the following command:

```bash
python main.py
```

## Requirements
This application does not require any external libraries or dependencies beyond Python itself.